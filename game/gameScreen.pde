

void gameScreen() {
  // array of accepted keys. Can be appended and removed
  //keys = new IntList();
  //resultList = new IntList();
  rectMode(CORNER);
  // Dummy BG
  background(255);
  // LEVEL 1: Four slots
  level(4);
  gameState = GAME;
}
